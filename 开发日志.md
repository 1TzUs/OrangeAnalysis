# 三国战报分析工具 — 开发日志

> 按时间倒序记录项目的关键开发节点、变更内容、验证方式与踩坑记录，
> 供后续开发回溯“为什么这么做”。详细技术原理见 [技术文档.md](./技术文档.md)。

---

## 2026-08-19 · 移除玩家名识别 + 新增兵力识别 + 去重规则调整

**背景**：简化识别字段（不再需要玩家名/id 区分同一阵容的不同玩家），新增兵力用于辅助展示与去重；去重规则不再依赖玩家 id。

**变更**：
- **移除玩家名识别（各部分同步）**：
  - [recognizer.ts](file:///e:/SoftWare/AICoding/Trae/three/server/src/recognizer.ts)：删除 `playerNames` 字段、`cleanPlayer`、`normalizePlayerNames` 及 `extractTopInfo` 中的玩家名提取；`Battle` 接口移除 `playerNames`。
  - [recognizer-portrait.ts](file:///e:/SoftWare/AICoding/Trae/three/server/src/recognizer-portrait.ts)：删除玩家名提取与 `pickSide` 中的玩家名部分。
  - [store.ts](file:///e:/SoftWare/AICoding/Trae/three/server/src/store.ts)：`BattleRecord` 移除 `playerId`。
  - 前端 [app.js](file:///e:/SoftWare/AICoding/Trae/three/server/public/app.js) / [style.css](file:///e:/SoftWare/AICoding/Trae/three/server/public/style.css) 与 [test-batch.ts](file:///e:/SoftWare/AICoding/Trae/three/server/src/test-batch.ts)：同步移除玩家名展示与问题标记。
- **新增兵力识别**：横/竖屏均已有 `x/y` 形式识别（如 `25000/30000`，含义为 **剩余/战前**），本次将其持久化到 `records.json` 新增 `hp` 字段；前端展示为「兵力 剩余25000 / 战前30000」。
- **去重规则调整**：[store.ts](file:///e:/SoftWare/AICoding/Trae/three/server/src/store.ts) 的 `recordKey` 由 `comp|stars|alliance|playerId|battleTime` 改为 `alliance|comp|stars|hp|battleTime`——同盟/阵容/红度/兵力/战报时间全部相同视为重复战报。

**验证**：`test-batch.ts all` 全量回归 45 张图无新增问题（仅剩已知的原图截断时间缺失、原图渲染武将不足3）；去重实测——同盟/阵容/红度/兵力/时间全同 → 去重 ✓，兵力不同 → 保留 ✓；`records.json` 新 schema（含 `hp`、无 `playerId`）读写正常。

**备注**：前端改动后需 **Ctrl+F5** 硬刷新；验证过程曾清空 records.json，已从服务内存恢复用户数据。

---

## 2026-08-19 · 玩家名识别增强与时间格式归一化

> ⚠️ 注：本节点中 `cleanPlayer` / `normalizePlayerNames` 及玩家名相关能力已于后续「移除玩家名识别」节点整体删除，仅时间格式归一化部分仍有效，此处保留作为历史回溯。

**背景**：PC 横屏识图偶发漏掉分隔符 `丨`（如 `风|只因` → `风只因`）；PE 竖屏将 `丨` 误读为 `一`（如 `雪|姜唐` → `雪一姜唐`），且时间格式粘连（`2026/08/1509:28:44`，日与时间之间缺空格）。

**变更**：
- [recognizer.ts](file:///e:/SoftWare/AICoding/Trae/three/server/src/recognizer.ts)
  - `cleanPlayer`：竖屏 `一/1` → `|` 的归一化正则（仅当“一/1”左侧 1–2 字前缀、右侧 ≥2 字才替换，避免误伤含“一”的普通名字）；去除首尾孤立 `|`。
  - 新增 `normalizePlayerNames`：同一玩家名在一张图内多次出现时，以带 `|` 的完整版本为准统一补全，保证持久化与去重口径一致；在 `parseBattleImage` 末尾调用。
  - `extractTime`：`TIME_PAT` 用 `\s*` 容忍日期与时间间缺空格，取出后统一补全为单空格（`2026/08/15 09:30:08`）。
- [recognizer-portrait.ts](file:///e:/SoftWare/AICoding/Trae/three/server/src/recognizer-portrait.ts)：竖屏同样接入 `cleanPlayer` / `extractTime` / `normalizePlayerNames`。

**验证**：`test-check.mjs` 批量回归——横屏 `PE/landscape/test1.jpg` 不再漏 `丨`；竖屏 `PE/portrait/test1.jpg` 时间归一化为 `2026/08/15 09:28:44`、`丨` 不再误读为 `一`；其余测试图无回归。

**备注**：前端改动后需 **Ctrl+F5** 硬刷新加载新 `app.js`。

---

## 2026-08-19 · 新增武将朱儁 + 战报时间识别 + 数据自动去重

**变更**：
- [dict.ts](file:///e:/SoftWare/AICoding/Trae/three/server/src/dict.ts)：词典新增群雄武将 `朱儁`。
- [match.ts](file:///e:/SoftWare/AICoding/Trae/three/server/src/match.ts)：`OCR_ALIASES` 增加 `朱俊→朱儁` 错字映射（儁/俊 形近）。
- 战报时间识别与持久化：
  - [recognizer.ts](file:///e:/SoftWare/AICoding/Trae/three/server/src/recognizer.ts) / `recognizer-portrait.ts`：`TIME_PAT` + `extractTime` 从战报底部 footer 区域识别时间。
  - [store.ts](file:///e:/SoftWare/AICoding/Trae/three/server/src/store.ts)：`BattleRecord` 新增 `ts`（时间戳，未识别为 null）、`battleTime`（原始时间串）、`playerId`（后随「移除玩家名识别」节点删除）；持久化时间取**战报上的时间**而非上传/识别时间。
  - [analysis.ts](file:///e:/SoftWare/AICoding/Trae/three/server/src/analysis.ts)：时间过滤兼容 `ts=null`（按范围外处理）。
  - 前端 `app.js` / `style.css`：展示战报时间与玩家名（玩家名展示后已随功能移除）。
- 数据自动去重：`store.ts` 的 `appendRecords` 以 `comp|stars|alliance|playerId|battleTime` 为键（后改为 `alliance|comp|stars|hp|battleTime`），完全相同则跳过不落盘。
- 旧数据兼容：`loadRecords` 自动补齐缺失字段（`ts` 非数字 → null，缺失字段 → 空串）。

**验证**：横竖屏截图时间识别正确（含粘连格式）；未识别时间时 `ts=null`、`battleTime=''`；相同阵容/红度/同盟/玩家/时间的重复战报被去重；前端无报错。

**备注**：修改识别/分析逻辑后需**重启 Web 服务**刷新 `store.ts` 内存缓存；未做新打包，前端需硬刷新。

---

## 早期里程碑（回溯记录）

### 打包为自包含 exe（一键启停 + 分发）
- **目标**：目标机无需安装 Node / Python 即可运行。
- **实现**：
  - OCR 服务：PyInstaller `--onefile --collect-all rapidocr_onnxruntime`（必须，否则 onnx 模型不打包导致运行时报错），产物 `ocr_service.exe`。
  - Web 服务：esbuild `--bundle --packages=external`（勿用 `--external=*`，会把相对模块也外置）→ `pkg` 打包；`pkg-config.json` 用 `assets` 收集 `@img/sharp-win32-x64` 原生包（缺它运行时报 `@img/sharp` 缺失）。
  - 路径适配：`process.pkg` 识别打包形态，`ROOT = dirname(process.execPath)` 保证 `data/`、`tmp/uploads/` 在 exe 外可写。
- **产物**：`build/dist-package/` 内含 `start.bat` / `stop.bat` / `README.md`，并压缩为 zip 分发。
- **踩坑**：杀软可能误报打包 exe，需加入信任区。

### Git 初始化与远程推送
- 初始化本地仓库、配置 `.gitignore`（排除 `build/`、`data/`、`tmp/`、`node_modules` 等），提交后推送到远程 GitHub 仓库。

### 技术文档编写
- 新建 `技术文档.md`，覆盖架构、核心模块、数据流、API、打包方式、常见陷阱与排查流程。

---

## 待办 / 规划

- [ ] 按新规则跑全量历史截图回归，确认无回归后重新打包分发。
- [ ] 视需要清理 `server/chi_sim.traineddata`（约 42MB 历史遗留，当前识别不走 Tesseract）。
- [ ] 补充更多测试图样本，持续扩展 `ALLIANCE_NOISE_SUFFIX` / `NOISE_SUFFIX` 与同盟名分隔符归一化规则。
