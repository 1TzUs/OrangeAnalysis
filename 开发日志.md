# 三国战报分析工具 — 开发日志

> 按时间倒序记录关键变更与踩坑，供回溯"为什么这么做"。技术原理详见 [技术文档.md](./技术文档.md)。

---

## 2026-09-07 · 主题切换圆形动画 + 滚动条统一

- **需求**：参考 Element Plus 官网，把深浅主题切换做成圆形过渡——暗→亮时亮色圆从主题按钮向外扩散盖住暗色，亮→暗时亮色向按钮回收露出暗色；并统一滚动条颜色，使其不再随深浅主题突兀变化。
- **初版**（`style.css` `::view-transition-*` + JS 在 `transition.ready` 用 `element.animate` 对 `::view-transition-new/old(root)` 做 `clip-path`）：在内置浏览器正常，但真实 Chrome 出现圆心不稳定（前两次偏、后面对）与切换瞬间闪白。
- **踩坑一 · 圆心偏移 / 闪白根因**：① 在 `transition.ready` 才挂动画有时序空隙，新视图会在动画挂上前整帧画出 →「点击瞬间已是浅色、无渐进」的闪白；② 用 CSS 变量（`--theme-x` 等）写进 `@keyframes` 时，部分真实 Chrome 不识别伪元素上的动态 `var`，`clip-path` 解析失败导致圆心整体偏（本机约偏左 200px）；③ 作用在 `old/new(root)` 快照层上时，其局部坐标原点在部分 Chrome 与视口不一致，也会造成圆心跑偏。
- **定型方案（注入式，规避以上三种）**：`app.js` `toggleThemeWithReveal` 在点击时把**具体像素**的 `clip-path` 初始值、方向 class（`theme-to-light` / `theme-to-dark`）、`@keyframes`（`vt-grow` / `vt-shrink`）动态写入临时 `<style id="theme-transition-style">`，再 `startViewTransition(applyTheme)`。伪元素创建时随 CSS 同步播放，无 JS 时序空隙；初始剪裁写死消除闪白；坐标直接用 `getBoundingClientRect` 的 px，圆心稳定在按钮上；方向明确满足「暗→亮扩散 / 亮→暗回收」。结束经 `transition.finished` 清理样式与 class。
  - `style.css` 只保留全局基线：`::view-transition-old/new(root){animation:none; mix-blend-mode:normal}` 关掉默认交叉淡入，其余全部由注入样式接管。
  - 注：曾试过统一作用在 `::view-transition-group(root)`（UA 固定满铺视口、坐标最稳）把旧视图向内收紧，圆心虽对但观感是「旧页被掏空露新页」而非 Element Plus 的「新层盖上来」，故弃用，回归 `new/old` 方向动画 + 注入坐标。
- **滚动条统一**：`color-scheme` 深/浅会让原生滚动条硬跳色。改为全局自制细滚动条——WebKit `::-webkit-scrollbar`（10px、透明轨道、`rgba(120,130,150,.4)` 圆角滑块、hover 加深）+ Firefox `scrollbar-width:thin` / `scrollbar-color`。用固定 RGB 而非 CSS 变量，保证深/浅主题颜色一致不变。
- **自验**：内置浏览器实测四连点深↔浅均正常切换、注入样式正确写入并结束后清理、控制台无 error；滚动条深/浅主题下均为统一淡灰。指纹 `app.js?v=20260907-36`、`style.css?v=20260907-19`。

---

## 2026-09-07 · 代码整洁与文档对齐（code-review 收尾）

- **术语统一**：「最大克星 / 最易得」→「最强对手 / 最弱对手」，README.md / 更新日志.md 与 `app.js` 端到端一致。
- **去重**：`battles.filter(b => Boolean(b.leftHp.trim()) || Boolean(b.rightHp.trim()))` 在 `recognizer.ts` / `recognizer-portrait.ts` 逐字重复 → 抽取共享 `export function hasHp(b)`（放 `recognizer.ts`，竖屏 `import` 复用）。
- **Primitive Obsession**：`trendFor` 的 `opts` 由 `number|string + Number()` 强转改为直接 `number`（路由已 `Number()` 化）。
- **前端去重**：`loadInsight` 与 `renderTrend` 重复拼 `URLSearchParams` → 抽 `insightParams(extra)`，两处复用公共筛选段。
- **基建**：`.trae/` 加入 `.gitignore`；技术文档补 `POST /api/records/clear-alliance` 路由与竖屏「底部遮挡容错」（兵力缺失置空 + `hasHp` 剔除）；为 `analysis.ts / recognizer.ts / recognizer-portrait.ts / index.html` 补文件尾换行。
- **验证**：`tsc --noEmit` 通过；浏览器实测洞察页（下拉/走势图/克制/窗口切换）与数据分析页删除确认框均正常，控制台无 error/warn。

---

## 2026-09-07 · 同盟删除（按同盟清空战报）

- **需求**：数据分析页同盟筛选标签提供删除入口，可删除某同盟的全部战报。
- **实现**：
  - `store.ts` 新增 `deleteAllianceRecords(alliance)`：按同盟精确过滤保留，返回删除条数；无匹配不写盘。
  - `index.ts` 新增 `POST /api/records/clear-alliance`：校验 `body.alliance` 非空后调用删除。
  - `app.js`：`renderAllianceChips` 为每个具体同盟 chip 右下角注入 `.chip-x`（trash 图标，仅同盟含、「全部」不含）；委托由容器 `#filter-alliance` 统一绑定 `.chip-x`→删除、其余→切筛选，**重渲染后仍有效且避免重复绑定**（替代原先每次渲染重新 forEach 绑定的写法）。删除流程带二次确认 `openConfirm`，成功后清理分析缓存、`refreshAllianceChips()`、`loadAnalysis()`。
  - `style.css`：`.chip-x` 绝对定位 chip 右上角，删除按钮**始终可见**（低透明、hover 增强）以兼容移动端无 hover。
- **注意**：删除是破坏性操作，前端强制二次确认；浏览器实测硬刷新后才加载到新 JS（业务缓存指纹已提升 `app.js?v=20260907-22`、`style.css?v=20260907-17`）。
- **迭代**：按用户要求精修删除前端——`.chip-x` 改为圆形小钮（右侧垂直居中、`border-radius:50%`、16px），常态低透明(0.45)、chip hover 显现、hover/聚焦危险红（`--lose`/`--lose-bg`，与 `.btn-danger` 一致）；`confirmDeleteAlliance` 确认框在提交前先统计该同盟条数，对话框注明「共 N 条战报」「删除后不可恢复」，危险按钮文案「删除 N 条」。指纹升至 `app.js?v=20260907-23`、`style.css?v=20260907-18`。

---

## 2026-09-07 · 竖屏同盟名误识别修复（底部遮挡场次）

- **Bug**：PC 竖屏战报**最底下一场战斗**（被底部导航条遮挡）的同盟名被识别成错误信息，如「壹一士兵甲」「币口」「周泰/周瑜」「雪一呆呆的踩」等（实为玩家名/武将名/导航残留）。
- **根因**（`recognizer-portrait.ts` `pickSide`）：同盟名借助**兵力行（x/y）锚定**取「兵力行上方最近候选」避免误吸底部导航。最底一场同盟/兵力行被导航遮挡后**兵力行缺失**，原逻辑退化为「取最下方候选行」——遮挡区残缺文本（玩家/武将/导航词）无法被 `UI_BUTTONS` 白名单过滤，直接被当同盟名。
- **修复**：`pickSide` 在**兵力行缺失时判定该侧战斗信息不完整，直接置空同盟名，不做臆测**（不再退化为取最下方候选）。有兵力行的正常场次逻辑不变。
- **自验**：用 `E:\GameViewer\Download\newbug\bug1-3.jpg` 实测，三张图底部被遮战斗双方同盟由错误文本变为 `""`，前三场正常战斗「百战|凌云」「风花雪月」保持不变，无回归。
- **后续**：按用户要求，**双侧兵力都为空（缺兵力数据）的战斗视为无效数据，整体剔除**（`recognizer-portrait.ts` / `recognizer.ts` 在 `return` 前对 `battles` 过滤 `leftHp`/`rightHp` 均为空者）。实测 bug1/bug2 战斗数由 4 → 3，被遮底部战斗剔除，正常战斗完整保留。注意：剔除放在 `return` 前而非精修 `continue`，以免破坏阶段2按 `idx` 回写的 `jobs` 下标对齐。

---

## 2026-09-07 · 洞察页（阵容走势 + 对战克制 + CSV/海报导出）

- **新增「洞察」tab**：`index.html` 增工具条（同盟 chips + 时间/兵力/走势窗口/阵容下拉 + 导出CSV/生成海报）+ 两张卡片（阵容走势、对战克制）。`activateTab` 补 `tabInsight` 切换并 `loadInsight()`。
- **后端走势 `trendFor`**（`analysis.ts`）：`passFilter` 改为导出供复用；按 `dayKey` 构造近 `days` 天日期桶，`ts` 非空 + comp 命中 + 任一侧武将 ≥3 + 过筛选的记录累加 `total/wins`，得 `TrendPoint[]`。`index.ts` 新增 `GET /api/analyze/trend?comp&days&alliance&hours&minHp`。
- **前端可视化**：走势用内联 SVG 矩形柱（`winRateColor` 上色、开窗按胜率缩放），克制用 `.matchup-row`（胜率色块 + 「最大克星/最易得」角标，基于 `matrix.cells` 纯前端计算，`total≥3` 才参与）。导出 `exportCsv`（前端拼 CSV，带 BOM）与 `exportPoster`（Canvas 绘排行海报，按当前主题深浅配色）均零后端依赖。
- **走势窗口下拉（重要 UX 修复）**：默认 `days=14` 太短，现有战报数据整体早于窗口（全为上月记录）导致一进页走势恒空态。新增「走势窗口」下拉（7/14/30/90），变更仅重绘走势并同步卡片说明；空态文案提示「可放大走势窗口或切换阵容」。放大到 90 天后能正确绘出该阵容 33 条记录。
- **踩坑**：`trendFor` 的 `points` 恒为 `days` 条（无数据日报 0），不能以 `points.length` 判空，须累加 `total` 判断；`Math.max(100,...)` 因胜率天然 ≤100 恒为 100，比重归一化无必要，`单日最高胜率` 应取 `Math.max(...winRate)` 而非依赖该三元判断。
- **UX 优化（测试后复检）**：① 走势图补稀疏日期刻度（`.trend-day`，首/中均分/尾最多 6 个 MM/DD），否则只见彩条不知日期；②「最新胜率」修正为「最近胜率」——取最近一场**有数据（total>0）**的日期，避免末位无数据日误显示 0%（此前现有数据止于上月，末位总为 0 → 恒显示 0%，严重误导）；③ 克制列表加表头（`.matchup-head`：阵容/场次/胜率），明确数字列语义；④ 海报无红度资料时平均红度显示「—」而非「0红」；⑤ CSV/海报文件名用本地日期（`locDate`），避免 `toISOString` UTC 差一天；⑥ `.sel-menu` 加 `max-width: min(340px, calc(100vw - 32px))` 防窄屏右缘溢出。
- **Bug 修复 · 切换阵容克制不更新（重要）**：洞察页「首选阵容」下拉点击选项后选中态与「对战克制」列表**均不变化**，恒定初始阵容。根因：`initSelect` 用 `opts = Array.from(menu.querySelectorAll('.sel-opt'))` **缓存了旧选项元素引用**并绑定点击监听，而 `loadInsight → buildCompMenu` 用 `innerHTML` 重建菜单后新选项**无监听**，点击事件落空。修复：`initSelect` 改为「菜单事件委托」（`menu.addEventListener('click')` + `closest('.sel-opt')`）+ 选项改为 `opts()` 动态查询，菜单重绘后新选项仍响应；同时修 `moveFocus`/Home/End 的缓存数组引用为动态。浏览器实测：切换各阵容下拉文字与克制列表同步更新，控制台无 error/warn。

---

## 2026-09-06 · 主题切换 + 全屏表格 + 设置自动保存 + 前端代码整顿

- **深浅主题切换**：`style.css` 维护两套 token（默认深色，`html[data-theme="light"]` 覆盖浅色）；右上角按钮切换并写 `localStorage.zabao.theme`。**FOUC 修复**：head 增内联脚本在 CSS 前按偏好预置 `data-theme`，`app.js` 的 `initTheme` 同步兜底，浅色偏好刷新不再闪深色。浅色主题下热力图/胜率色块文字转深色保证可读。
- **标识图标浅色对比度（关键修复）**：设置页四标识标题图标原硬编码浅暖色（`#ffb36b` 等），浅色背景下发白难辨。补齐 `[data-theme="light"]` 深一档覆盖色（深棕/暗金/深红/暗褐），浏览器实测计算色值一致、清晰可辨。
- **全屏表格（`#fs-view`）**：胜率排行/热力图标题右侧「展开」铺满窗口；全量渲染（不截断「前 30」）；独立筛选栏；表头可点按胜率升/降序；顶部子页签两表互切；右上「关闭」或 Esc 退出并恢复底部表格、锁定滚动。表格紧凑高密度（行 padding 5px 8px、字号 13px、热力图格 2px 3px），一屏容纳更多。
- **设置自动保存**：移除「保存」按钮，`collectSettings`（含越界 clamp）+ `autoSaveSettings` 绑各控件 `change`，写入 `localStorage` 后弹「已保存 ✓」。toast 改 `position:fixed` 底部居中，任何位置修改都可见。
- **范围提示统一**：删除 `.setting-hint.range` 差异化样式，全部统一文案「范围 X ~ Y，0 = 不限」与同一圆点、颜色、字号。
- **默认口径自动套用**：`applyDefaultCaliber` + `selectLargestAtMost` 在进入分析页/保存设置后把设置默认口径套到筛选器（`caliberApplied` 标记防重复），修正改口径后排行不联动的问题。
- **安全**：`setStatus` 拼 `innerHTML` 前经 `escHtml` 转义 `<>&`，杜绝后端错误信息注入。
- **键盘可达性**：`initSelect` 补方向键移动、Home/End 首尾、Esc 收起并归还焦点到触发区。
- **坏味道重构**：抽 `sortHeaderBtn/nextSort/byWinRate`（主页面与全屏共享排序逻辑）、`appendThresholdParams`（`loadAnalysis`/全屏复用阈值下发）、封装 `fsState` 状态对象（替代 4 个零散全屏变量）、`pickOptionInto`→`selectLargestAtMost`。
- **图标化**：全局 emoji 标识替换为单色 SVG（`ICONS` 集中统一、`currentColor` 随主题）；.sel 下拉替代原生 `<select>`。
- **测试文件归位**：`server/test-recognize.ts` → `server/tmp/`（已 gitignore），输出改落该目录，避免测试产物与绝对路径污染 git 状态。

## 2026-09-01 · 武将名单解耦 + 赛季版本 + 设置页整理

- **名单数据解耦**：内置名单从 `dict.ts` 迁到新 `generals-data.ts`（同构 `{version, generals}`，兜底）。`dict.ts` 只留停用词/同盟等匹配规则；`match.ts` 改经 `getGenerals()` 用运行时生效名单（识别词库与名单同源，不动识别侧）。
- **版本号改赛季制**：`2026-08-28` → `s16-2026-09-01`（新武将随赛季发布）。`cmpVersion` 重写为解析 `{season,date}`，**先比赛季、同季再比日期**；旧纯日期视为赛季 0。
- **防降级（关键修复）**：原 `update` 仅判 `sameVersion && sameList`，会把远程旧格式（赛季 0）**降级覆盖**内置 `s16`。改为仅当 `cmpVersion(chosen,current) > 0` 才热替换；否则保留现有。避免陈旧/降级源覆盖新赛季版本。
- **API**：`/api/generals` 新增 `season` 字段（解析自当前生效版本）。
- **前端**：设置页「武将名单」卡片移到最底端、只显示赛季（`S16`，无赛季/内置显示「内置默认」）、去掉武将数量；更新弹窗改为完整版本（去 `v` 前缀）。
- **设置页整理**：「标识体系」快速升温/白板之光/泥头车/陷阱四组合并为原生 `details` 可折叠抽屉（默认折叠），子项改浅色圆角小卡，整体风格统一。

## 2026-08-25 · 云端同步 + 去重上传 + 清空云端

- **云端同步（JSONBin.io）**：顶部「本地数据 / 云端数据」下拉。路由：`GET /api/cloud/pull`（云端覆盖本地）、`POST /api/cloud/push`（合并去重上传）、`POST /api/cloud/clear`（口令校验清空），见 `src/cloud.ts`。Key 仅存服务端，可被 `CLOUD_ACCESS_KEY`/`CLOUD_MASTER_KEY` 覆盖。
- **上传去重（不再覆盖）**：push 先 `fetchRecords` 读云端，再 `store.mergeRecords(cloud, local)` 按 `recordKey` 去重合并（云端保留、仅并入本地新增），返回 `total/added/skipped`，整体写回。避免直接覆盖丢云端已有记录，语义与「导入数据」一致。
- **JSONBin 空值陷阱**：JSONBin 拒绝空 record（`[]` → `400 "Bin cannot be blank"`）。清空云端改写入占位 `{"records":[]}`，`fetchRecords` 兼容读回为空数组。
- **清空云端（口令）**：`/api/cloud/clear` 校验服务端常量 `CLOUD_PASSWORD`（默认 `orange`），错误返回 403；前端不存口令，`openPassword` 弹窗输入，成功/失败均弹 `openAlert` 反馈。
- **前端弹窗化**：`openConfirm/openAlert/openPassword` 替代原生 confirm/alert；设置页新增「☁️ 云端数据」危险卡片；导入/导出/云端按钮整合为「本地数据 / 云端数据」下拉。
- **踩坑**：① `#modal` 曾嵌套在 `#tab-parse` 内，切页后父级 `display:none` 导致弹窗不可见，须移到 `<main>` 直接子级；② 密码框用 `hidden` **属性**而 JS 移除的是 `class="hidden"`（属性≠class），始终不显示，须 `removeAttribute('hidden')`。
- **静态缓存**：前端资源 URL 加 `?v=20260826-N` 强制刷新，避免改 JS 后浏览器仍用旧文件。

## 2026-08-21 · 徽标体系 + 设置页 + 识别优化

- **泥头车判定改 `>=`**：`s.truck` 由 `winRate > truckRate` 改为 `winRate >= truckRate`（含边界值），前后端文案与 API 文档同步为「≥60%」。
- **白板之光改按分段红度**：判定从「三将逐将平均红度之和 ≤ wbStars」改为按**最低红度段「0-5红」**——该段场次 ≥ `wbMin` 且该段胜率 ≥ `wbRate`（默认 5 场 / ≥51%）。`wbStars`（红度上限）参数废弃，从 `badge`/设置页/前后端透传一并移除。
- **新增「陷阱」徽标**：`CompStat.trap` = 总场次 ≥ `trapMin`（默认 20）且总体胜率 < 50%，提示谨慎使用。`#trapMin` 经 `/api/analyze` 透传，设置页独立配置，`.trap-badge` 暗褐/土金警告色。
- **徽标显示开关**：快速升温/白板之光/泥头车/陷阱四个徽标各配 `settings-head` 拨动开关（`set-*-show`），默认开启；`rankBadges` 据 `localStorage` 决定是否渲染，存 `zabao.settings`。
- **场次门槛 0 值修复**：`wbMin/truckMin` 显式填 `0`（不限）会被前端 `x || 默认` 与后端 `Number(x) || 默认` 误回退成 5。改为仅当参数为空串/缺失才回退默认，`0` 原样生效。前端 `fillSettingsForm` 对 checkbox 改用 `.checked` 回填。
- **快速升温**：近 `HOT_MS` 内新出现、≥`HOT_MIN` 场、出场占比 ≥ `HOT_RATE` 的阵容打「🔥 快速升温」标并支持悬停解释。阈值由 `/api/analyze` 透传 `hotMin/hotRate/hotHours` 下发（默认近 3h / ≥5 场 / ≥10%）。
- **场次筛选**：`analysis.ts` 新增 `minCount`（阵容总场次下限）；分析页加「场次」下拉。
- **配对重构**：同盟筛选无数据修复——战队配对由「相邻对」改为**按 `image+ts+battleTime` 分组配对**，筛选后单条记录即可计入，整组剔除任一侧武将 <3 的战斗。
- **胜率颜色**：`winRateColor` 改连续渐变，55% 以上直接封顶深绿（T0 顶级队一眼可辨）。
- **设置页**：新增「⚙️ 设置」页签——有效战报默认口径（最低场次 / 最低兵力手动输入，兵力 0–33000）+ 快速升温门槛；设置存 `localStorage`；分析页兵力/场次筛选与设置**独立**，不自动套用默认口径。
- **前端体验**：标签页切换写 `zabao.tab`，刷新后停留原页面；设置提示改为悬浮气泡不挤压按钮；新增底部版权 `© 2026 欧润吉 All rights reserved`（flex 贴底）。
- **解码去重提速**：`ocr_service.py` 加整图解码缓存（LRU），整图与各区域 OCR 复用同一像素数组；`red.ts` 缓存 sharp 原始像素，逐武将取色不再重复解码整图。实测横屏单图快约 18%、竖屏快约 10%，精度逐字节不变。
- **红度回归修复**：`red.ts` 内存切片的连通域 BFS 邻居偏移多叠加一次 `y0/x0`，勾玉无法连通、红度恒 0；已修复并联查比对通过。
- **并行推理踩坑（结论）**：尝试 `ThreadingHTTPServer` + 图内/多图并行，反而变慢。根因：纯 CPU 下单次 onnxruntime 推理已占满线程池，并行只是排队/线程争用，多图并行实测 17.4s vs 17.7s 无收益。顺序处理保持不变；仅换 GPU 推理才有并行空间。
- **查阅**：设置相关代码见 `public/index.html`（`#tab-settings`）与 `public/app.js`（`settingsEls`/`activateTab`）。

## 2026-08-20 · 逐武将红度 + 兵力筛选 + 布局优化

- **逐武将红度**：`BattleRecord` 新增 `compReds`（与阵容顺序一致的逐武将红度）；分析输出 `genReds`（各武将平均红度 + 场次数）；排行/热力图武将 chip 右侧显示平均红度角标。去重键改用 `compReds`（红度分布不同不再误去重）。
- **兵力筛选**：`/api/analyze` 新增 `minHp`（按战前兵力 `hpBefore` 过滤）；前端加"兵力"下拉（不限/20000/25000/30000 以上）。
- **修复同盟筛选无数据**：记录按左右相邻成对写入，筛选后相邻两条不再同场，配对被整体跳过。改为**按战斗分组配对**（同 `image+ts+battleTime` 为一组），筛选同盟后单条记录即可计入，矩阵从组内未命中记录取对手。
- **兵力拆分为战前/战后**：`hp`（`剩余/战前` 拼接串）拆为 `hpAfter`（战后剩余）+ `hpBefore`（战前）；`splitHp` 解析；`loadRecords` 兼容旧数据自动迁移。
- **UI**：压缩排行/矩阵列宽（分档列 40px、胜率条 54px、矩阵 cell 34px），阵容列 `nowrap` 不换行，消除横向滚动条；筛选区按钮右对齐（`toolbar-actions`）。
- 验证：同盟筛选均返回数据（宇宙洪荒/剑来 10、百战|凌云 3、风花雪月 2）；浏览器无横向滚动、无 JS 报错。

## 2026-08-19 · 识别增强 + 去重调整

- **移除玩家名**：移除玩家名识别（recognizer/recognizer-portrait/store/前端同步删）；新增兵力识别并持久化 `hp`（后拆分为两字段）。
- **去重键调整**：`comp|stars|alliance|playerId|battleTime` → `alliance|comp|stars|hp|battleTime`（后改为含 `compReds`）。
- **玩家名增强 + 时间归一化**：玩家名分隔符 `丨` 归一化（竖屏 `一/1→|`）、`normalizePlayerNames` 统一同图重复名；`extractTime` 补全日期与时间间缺失空格（如 `2026/08/1509:28:44`）。
- **朱儁 + 战报时间识别 + 数据去重**：词典新增 `朱儁`，别名 `朱俊→朱儁`；持久化战报时间（`ts`/`battleTime`），时间过滤兼容 `ts=null`；`appendRecords` 按键去重不落盘。

## 早期里程碑

- 打包自包含 exe（PyInstaller + esbuild + pkg，收集 sharp 原生包与 onnx 模型；start/stop.bat 一键启停）。
- Git 初始化与远程推送；编写技术文档.md。

---

## 待办

- [ ] 按新规则全量回归后重新打包分发。
- [ ] 清理 `server/chi_sim.traineddata`（约 42MB 遗留，识别不走 Tesseract）。
- [ ] 持续补充同盟名噪声后缀与分隔符归一化规则。