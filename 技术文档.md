# 三国战报分析工具 — 技术文档

> 面向后续开发维护者。本文介绍项目架构、核心模块、数据流、运行/打包方式及常见陷阱。
> 适用语言：中文；默认工作目录：`server/`。

---

## 1. 项目简介

本项目用于解析《三国：谋定天下》游戏战报截图，从中识别出：

- 每一场战斗的**双方阵容**（3 名武将）
- 双方所属**同盟**
- 双方**兵力**（识别层为 `剩余/战前` 字符串如 `25000/30000`；持久化时拆分为战后剩余与战前两个字段，用于去重与辅助展示）
- **战报时间**（截图上的战斗时间，用于时间过滤与去重）
- **战斗结果**（胜 / 败 / 平）
- 每个武将的**红度**（名称上方的勾玉数量，0–5）
- 体力消耗（辅助信息）

识别结果持久化后，前端提供两张分析视图：

- **阵容胜率排行**：各阵容总场次 / 胜率 / 平均红度 / 逐武将平均红度 / 红度分档胜率。
- **对战热力图**：两两阵容对阵的胜率矩阵（行/列单位取场次前 15 的阵容）。

分析页支持**战前兵力筛选**（`20000` / `25000` / `30000` 以上，均按战前兵力 `hpBefore` 过滤）。

### 硬性约定（勿改动）

| 项 | 值 |
|---|---|
| Web 服务端口 | `3001`（页面标题/主标题见下文） |
| OCR 服务端口 | `127.0.0.1:5050` |
| Web 页面标题 | `战报分析` |
| 主页标题 | `⚔️ 战报分析` |

---

## 2. 技术架构

整体为 **双进程 + HTTP 通信**：

```
浏览器 (public/*)
   │  HTTP
   ▼
Web 服务 (Node.js/Express, :3001)   ← 入口 server/src/index.ts
   │  ├── 图片上传(multer) → tmp/uploads/
   │  ├── 识别链路 (recognizer*.ts)
   │  │        │
   │  │        ▼  HTTP POST /ocr (图片路径 + 裁剪框 + scale)
   │  │   OCR 服务 (Python RapidOCR, 127.0.0.1:5050)   ← py/ocr_service.py
   │  │        │  返回带坐标的文本行
   │  │        ▼
   │  │   解析成 Battle { 双方武将/同盟/兵力/结果/红度 }
   │  ├── 持久化 store.ts → data/records.json
   │  └── 聚合分析 analysis.ts
```

- **Web 服务**（TypeScript，`tsx` 运行）负责上传、识别编排、持久化、对外 API、托管静态资源。
- **OCR 服务**（Python `RapidOCR` 独立进程）只做“文字识别”，不感知业务；Web 端以 HTTP 请求指定裁剪区域做整机/区域识别。

> 依赖外部 OCR 进程是刻意的：`icu/onnx` 模型体积大、初始化耗时，独立常驻进程可复用模型、避免每次识别重复加载。

---

## 3. 环境依赖

### 开发环境
- Node.js ≥ 20（含 npm）
- Python ≥ 3.10，`pip install rapidocr_onnxruntime opencv-python-headless numpy`
- Rust 无关；OCe 底层库由 npm 包自带

### server/package.json 关键依赖
| 包 | 用途 |
|---|---|
| `express` | Web 服务框架 |
| `multer` | 图片上传（表单，磁盘存储） |
| `sharp` | 图像读取 / 裁剪 / metadata（含原生模块，打包见 §9） |
| `tesseract.js` | 仅预留，当前识别主走 RapidOCR 服务 |
| `cors` | 跨域（开发调试用） |
| `tsx` | 直接运行 TS（`dev` 脚本启用 watch） |
| `@yao-pkg/pkg` | 将 Web 服务打包为自包含 exe（§9） |

---

## 4. 快速开始

```bash
# 1) 启动 OCR 服务（占用 5050）
cd server
python py/ocr_service.py 5050          # 或 python py/ocr_service.py（默认 5050）

# 2) 另开终端，启动 Web 服务（占用 3001）
cd server
npm install
npm run dev                            # 热重载，或 npm run start

# 3) 浏览器打开
http://localhost:3001
```

> **修改了识别/分析逻辑后，务必重启 Web 服务**——`store.ts` 对 `records.json` 有内存缓存（`cache` 变量），不重启不会读取磁盘新数据。

---

## 5. 目录结构

```
three/
├── .gitignore
├── ocr_service.spec            # PyInstaller 打包 OCR 服务的 spec
├── build/                      # 打包中间产物（不入库）
├── server/                     # 主工程
│   ├── package.json / tsconfig.json / package-lock.json
│   ├── pkg-config.json         # pkg 打包 Node 配置（收集原生模块）
│   ├── probe.mjs               # 调试脚本
│   ├── data/records.json       # 持久化战斗记录（运行时生成，不入库）
│   ├── public/                 # 前端静态资源（index.html / app.js / style.css / orange.jpg 图标）
│   ├── py/                     # Python OCR 服务
│   │   └── ocr_service.py      # RapidOCR HTTP 服务（唯一入口）
│   ├── src/                    # TypeScript 源码
│   │   ├── index.ts            # 服务端入口（Express，API，静态托管）
│   │   ├── store.ts            # 记录持久化 + 数据模型转换
│   │   ├── analysis.ts         # 阵容胜率 / 对战矩阵聚合
│   │   ├── ocrService.ts       # OCR HTTP 客户端（整图/区域）
│   │   ├── recognizer.ts       # PC 横屏识别核心
│   │   ├── recognizer-portrait.ts # PE 竖屏识别
│   │   ├── red.ts              # 红度（勾玉）识别
│   │   ├── match.ts            # 武将词典模糊匹配
│   │   ├── dict.ts             # 全部有效三国武将名单
│   │   └── *_test.ts / probe-*.ts / tmp-*.ts   # 调试/验证脚本（可删可留）
│   ├── tessdata/chi_sim.traineddata   # Tesseract 中文包（预留下游用）
│   ├── chi_sim.traineddata     # （历史遗留，见 §11）
│   └── tmp/uploads/            # 上传图片临时存储（运行时生成，不入库）
```

---

## 6. 核心模块详解

### 6.1 服务端入口 `src/index.ts`
- 通过 `__dirname` / `ROOT` 适配开发与打包两种运行形态（见 §9）。
- `UPLOAD_DIR = ROOT/tmp/uploads`，`PUBLIC_DIR = ROOT/public`。
- 上传限制：仅图片（png/jpg/webp/bmp），≤20MB。

API（详见 §8）：
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/parse` | 单图识别（`mode=pc` 横屏 / `portrait` 竖屏） |
| POST | `/api/parse-many` | 多图识别（字段 `images`，最多 20 张，顺序执行） |
| GET  | `/api/analyze` | 聚合统计（`alliance` / `hours` 过滤） |
| GET  | `/api/records` | 返回全部原始记录 |
| POST | `/api/records/clear` | 清空全部记录 |
| ——  | `/uploads/*` | 已上传图片静态访问（缩略图） |

### 6.2 识别链路

#### `ocrService.ts` — OCR 客户端
- 向 `127.0.0.1:5050/ocr` POST `{image, x0, y0, x1, y1, scale}`。
- `ocrImage(path, scale)`：整图识别；`ocrRegionService(path, box, scale)`：区域识别。
- 返回 `OcrLine[]`，坐标为**原图坐标**（坐标还原在 Python 侧完成）。

#### `recognizer.ts` — PC 横屏识别（核心）
流程：
1. `sharp` 取宽高，`cx = W/2` 分左右。
2. 整图 OCR 一次，得到全部文本行。
3. **结果锚点**：字形足够大的单字 `胜/败/平`（`isResultGlyph`，高度阈值 = 0.5×fontH，fontH ≈ 0.1×H），按 y 排序。
4. 相邻锚点中点切分各战斗的纵向范围。
5. **完整战斗判定 `isTopTruncated`**：合并两条——结果字上方有成排 UI 导航词（`UI_NAV_WORDS`）**且**无任何战斗内容行（同盟名）→ 判定该战斗从列表中间截取，剔除。
6. **同盟名 `extractTopInfo`**：在战斗顶部条带内，优先找旁带“盟”徽标的行作为同盟名；缺失回退到区域最下方一行。同盟名经 `cleanAlliance` 归一化（竖线/漏分隔符/噪声后缀），末尾含噪声字时再 `refineAlliance` 高倍精修。
7. **武将带**：结果字下方 ~80px 区域高倍（scale=2）OCR，`classifyGeneralBand` 解析：体力消耗、兵力（`x/y`）、武将名（`matchGeneral` 词典匹配），按左右归并并去重。
8. **红度 `fillRed`**：对每个武将调用 `red.ts`。
9. **战报时间 `extractTime`**：时间位于战斗面板顶部（右上角，与“进攻/防守第X场/坐标”同行），即结果字**上方**区域；取结果字上方离其最近的一条符合 `TIME_PAT` 的行，`extractTime` 归一化（补全日期与时间间缺失的空格）。

##### 战报时间 `extractTime`
- `TIME_PAT = /\d{4}[\/\-]\d{1,2}[\/\-]\d{1,2}\s*\d{1,2}:\d{2}(?::\d{2})?/`，日期与时间间分隔符用 `\s*` 容忍 OCR 粘连（如 `2026/08/1509:30:08`）。
- `extractTime` 取出匹配串后再用正则补全为单空格：`2026/08/15 09:30:08`。未识别则返回空串。

#### `recognizer-portrait.ts` — PE 竖屏识别
适配竖屏（如 1080×2376 单列面板）。与横屏差异：
- 结果字位于面板**顶部/偏上**。
- 顺序向下为：武将带 → 同盟名 → 兵力。
- 武将带取**最顶部一行文本簇**（`topRowCluster`），避免下方同盟名等行模糊匹配混入武将。
- 同盟名在武将带**下方**查找，用兵力（`x/y`）行锚定——取兵力行上方最近候选行；并排除底部导航词（`返回/同盟/战报` 等）。

#### `match.ts` / `dict.ts` — 武将模糊匹配
- `OCR_ALIASES`：已知形近错字 → 正确武将 的**高优先映射表**（如 `曹不→曹丕`、`张合→张郃`、`荀或→荀彧`、`朱俊→朱儁`）。映射优先于编辑距离，可避免歧义（如“曹不”到曹操/曹丕距离同为 1）。
- `matchGeneral(raw)`：清洗后，先查别名表，再用 **Levenshtein 编辑距离**遍历 `dict.ts` 全词典，取最优；长度差 ≤1 且距离 ≤ 阈值（2 字名 ≤1，更长名 ≤ floor(len/3)+1）才返回。
- 新增武将：同时更新 `dict.ts`（词典）——**必须包含全部有效三国武将**（如 郝昭、郭淮、文聘、曹彰、卞夫人、**朱儁**），否则匹配不到；若存在常被 OCR 读错的形近字，还需在 `OCR_ALIASES` 补一条映射。

#### `red.ts` — 红度（勾玉）识别
- 勾玉特征：橙红色（`isMagColor`）实心图标，紧贴武将名正上方横向等间距排列；空心结构 → 连通域拆成“大面积主块 + 1~3px 散点”。
- `collectBlocks`：对武将名上方窄带做橙红色**连通域分析**，返回各块（bbox + 面积）。
- `countRedForGeneral`：
  - 勾玉尺寸按武将名高 `nameH` 缩放：宽≈0.6× 名高、高≈0.8× 名高、中心距≈0.8× 名高。
  - 扫描带 x 范围外扩到**2×勾玉中心距 + 半勾玉宽**（保证边缘勾玉不被截断漏计）。
  - 面积阈值 = max(0.1×勾玉面积, 12)，剔除散点碎片。
  - 粘连宽块按 `w / estSpace` 拆分计数；结果上限 5。

### 6.3 持久化 `store.ts`
- `records.json` 每张识别后的战斗拆成**两条**记录（左方视角 + 右方视角），便于按任意阵容/同盟统计胜率。
- 单条 `BattleRecord`：
  ```ts
  { comp: string;      // 3 武将名按字典序 "/" 拼接，如 "周瑜/诸葛亮/诸葛瑾"
    compReds: number[]; // 逐武将红度（0-5，未知 -1），顺序与 comp 中武将一致；旧数据缺失为空数组
    alliance: string;  // 该方所属同盟
    result: 'win'|'lose';
    stars: number;     // 总红度(0-15)，含未知武将则为 -1
    ts: number | null;      // 战报时间戳（解析自战报上的时间），未识别为 null
    battleTime: string;     // 战报原始时间串（如 "2026/08/15 09:35:48"），未识别为空串
    hpAfter: string;        // 战后剩余兵力，如 "25000"，未识别为空串
    hpBefore: string;       // 战前兵力，如 "30000"，未识别为空串
    image: string; }
  ```
- 识别层的 `leftHp`/`rightHp` 为 `剩余/战前` 拼接串（如 `25000/30000`），持久化时由 `splitHp` 拆分为 `hpAfter`（战后剩余，取斜杠前）/`hpBefore`（战前，取斜杠后）两个字段。
- 关键过滤：任一侧识别武将 **< 3 个** → 整场剔除（`battleToRecords`）。
- **持久化时间取战报上的时间**（`battle.time`），而非上传/识别时间；未识别则 `ts=null`、`battleTime=''`（`parseBattleTime` 兼容斜杠/短横线格式）。
- **自动去重 `appendRecords`**：去重键 = `alliance|comp|compReds|hpAfter|hpBefore|battleTime`（`recordKey`），同盟/阵容/逐武将红度/兵力(战前/战后)/战报时间全部相同视为重复战报，追加时跳过且不落盘。
- `compOf(generals)`：武将名排序去重生成阵容 key（顺序无关），同时返回与 key 顺序一致的逐武将红度数组 `compReds`（由 `battleToRecords` 写入记录）。
- 兼容旧数据：`loadRecords` 对缺失 `compReds` 的记录补空数组（无法反推逐武将红度，分析/展示退化为不显示角标）。

### 6.4 聚合分析 `analysis.ts`
- 入口 `analyze(alliance, hours, minHp=0, minCount=0, hot)`；`hot={min, rate, ms}` 为快速升温阈值配置（默认近 3h / ≥5 场 / ≥10%）。
- `passFilter(alliance, hours, minHp)`：按 `alliance`（精确相等）、`hours`（时间窗口）、`minHp`（战前兵力下限）过滤；`ts=null`（战报时间未识别）的记录按范围外处理，`minHp>0` 时兵力未识别或 `hpBefore<minHp` 剔除。随后按 `minCount` 过滤总场次低于阈值的阵容。
- **战斗配对（同盟筛选修复）**：同 `image + ts + battleTime` 的记录归为一组（一场战斗的左右两条视角）；任一侧识别武将 <3 的整组剔除，避免污染统计与矩阵配对。筛选后逐条计入（无需成对），矩阵对手从筛选命中组内记录取。
- **红度分档** `STAR_BRACKETS`：`0-5` / `6-8` / `9-11` / `12-14` / `15` 红。
- **逐武将平均红度 `genReds`**：每个 `CompStat` 含 `genReds: GeneralRedStat[]`（`name`/`avgRed`/`known`），顺序与 `comp` 中武将一致；`compReds` 中 `-1`（未识别）不计入平均，无有效数据时 `avgRed=0`、`known=0`。
- **快速升温 `hot`**：`CompStat.hot` 为 true 当且仅当——近 `hot.ms` 内无早于窗内的旧记录（新出现）、近 `hot.ms` 出场数 ≥ `hot.min`、且近 `hot.ms` 出场占比 ≥ `hot.rate`；`hotCount` 为非空近窗场次。阈值由 `/api/analyze` 透传，前端设置页可调。
- **标识判定（`badge` 配置）**：`/api/analyze` 透传 `wbRate/wbMin/truckRate/truckMin/trapMin`。四个徽标互斥判定：
  - **白板之光 `whiteBoard`**：按**分段红度**（非总体红度），取最低红度段 `0-5红`，该段场次 ≥ `wbMin` 且该段胜率 ≥ `wbRate`（默认 5 场 / ≥51%）。
  - **泥头车 `truck`**：总场次 ≥ `truckMin` 且总体胜率 ≥ `truckRate`（默认 ≥60%）。
  - **陷阱 `trap`**：总场次 ≥ `trapMin`（默认 20）且总体胜率 < 50%，提示谨慎使用。
  - `wbMin/truckMin/trapMin` 可显式填 `0`（不设场次门槛）；仅当参数为空串/缺失才回退默认值。
- 输出 `comps`（按场次降序、再胜率降序）与 `matrix`（取场次前 15 阵容做行列，含反向单元的对称累计）。

### 6.5 `py/ocr_service.py` — OCR 服务
- `RapidOCR` 单例懒加载（首次请求才初始化模型）。
- `POST /ocr`：读 `image` 绝对路径 → `cv2.imread`（注意 Windows 中文路径会返回 `None`，见 §11）→ 裁剪 → 缩放 → 识别 → 坐标**除以 scale 并加回偏移**还原到原图 → 返回 `{lines}`。

---

## 7. 前端 `public/`

单页应用，无构建步骤（原生 JS，直接浏览器加载）。四个主要区块：

- **识别页（tab-parse）**：上传/拖拽图片（`/api/parse`，可选 `portrait` or `pc` 模式），逐张或批量显示每张图的战斗卡片（双方武将 chips、同盟、兵力（`剩余/战前`）、红度标记、结果）。
- **分析页（tab-analyze）**：
  - 筛选（`filter-alliance` 联盟、`filter-hours` 时间、`filter-hp` 兵力下限、`filter-count` 场次下限）→ 调 `/api/analyze`，同时携带设置页的快速升温阈值 `hotMin/hotRate/hotHours`。兵力/场次筛选与设置页**独立**，不套用设置默认口径。
  - `renderRanking` 渲染阵容胜率排行（含红度分档列，可配色）。
  - `renderMatrix` 渲染对战热力图（行/列头共享 `compChips` 渲染逻辑）。
  - **逐武将平均红度角标**：`compChips(comp, genReds)` 将阵容拆为武将 chip，某武将 `known>0` 时在其名右侧显示橙色 `avgRed` 角标（`i.gred`）；排行表与矩阵行列头均显示。
  - 热力图连表颜色：0% `rgb(190,30,30)` → 50% `rgb(245,245,245)` → 100% `rgb(30,130,50)` 连续渐变。
  - **标识徽标（`rankBadges`）**：按 `hot`/`whiteBoard`/`truck`/`trap` 字段在场次右侧渲染「🔥 快速升温」「🔆 白板之光」「🚚 泥头车」「⚠️ 陷阱」徽标，悬停展示判定文本。每个徽标可在设置页独立开关显示（`hotShow/wbShow/truckShow/trapShow`，存 `localStorage`）。
  - **胜率颜色 `winRateColor`**：<50% 从深红到浅红、>50% 浅绿到深绿连续渐变，≥55% 直接封顶深绿（T0）；总胜率列用渐变徽章。
- **设置页（tab-settings）**：有效战报默认口径（`set-count` 最低场次、`set-hp` 最低兵力手动输入，兵力范围 0–33000）+ 快速升温门槛（`set-hot-hours/min/rate`）+ 白板之光/泥头车/陷阱门槛（`set-wb-*/set-truck-*/set-trap-min`）及各自显示开关（`set-*-show`）。设置存 `localStorage`（`zabao.settings`），保存后随 `/api/analyze` 下发。
  - 标签页切换写入 `zabao.tab`，页面刷新后恢复原停留页；设置提示（`settings-toast`）为绝对定位悬浮气泡，不挤压按钮；底部版权 `© 2026 欧润吉 All rights reserved`（body flex 贴底）。
  - 注意：`renderAnalysis` 在数据为空时要清理残留 DOM（`dataset.last` 缓存），否则清空后切换 tab 仍显示旧数据。若遇到此类"改了不生效"，先 **Ctrl+F5 硬刷新**排除静态 JS 缓存。

`index.html` / `style.css`：标题、`.
app-logo`/`.app-title`（图标 + 标题）、模式切换按钮、热力图配色等视觉规范。

---

## 8. API 参考

### POST /api/parse
- 表单：`image`（文件）、`mode`（可选，`pc` | `portrait`，默认 `pc`）。
- 响应：
  ```json
  {
    "mode": "pc",
    "image": "/uploads/xxx.png",
    "imageWidth": 1920, "imageHeight": 1080,
    "battles": [{
      "leftGenerals": [{"name":"曹丕","raw":"曹丕","x":..,"y":..,"nameW":..,"nameH":..,"red":2}],
      "rightGenerals": [ ... ],
      "leftAlliance": "百战|凌云", "rightAlliance": "风花雪月",
      "time": "2026/08/15 09:35:48",
      "result": "win"|"lose"|"unknown", "resultText": "胜",
      "leftHp": "5000/5000", "rightHp": "3000/5000", "hpCost": "30",
      "panel": {"y0":0,"y1":400}
    }]
  }
  ```
- `time` 未识别时为空串；兵力 `leftHp`/`rightHp`（`剩余/战前`，如 `25000/30000`）未识别时为空串。
- 识别成功的记录会被**自动追加持久化**到 `data/records.json`（去重键见 §6.3）。

### POST /api/parse-many
- 表单字段名 `images`，可多选（≤20）。响应 `{items:[...]}`，顺序（非并发）执行。

### GET /api/analyze?alliance=xxx&hours=N&minHp=N&minCount=N&hotMin=N&hotRate=N&hotHours=N&wbRate=N&wbMin=N&truckRate=N&truckMin=N&trapMin=N
- `alliance` 空 = 全部；`hours=0` 或缺失 = 不限时间；`minHp=0`或缺失 = 不限战前兵力（否则只统计 `hpBefore ≥ minHp`）；`minCount=0`或缺失 = 不限阵容总场次。
- `hotMin/hotRate/hotHours` 为前端设置页下发的快速升温阈值（默认 5 场 / 10% / 3 小时），未传时回退默认。
- `wbRate/wbMin`（白板之光，默认 ≥51% / 5 场）、`truckRate/truckMin`（泥头车，默认 ≥60% / 5 场）、`trapMin`（陷阱，默认 20 场）。三个场次参数均支持显式 `0` = 不限，仅空串/缺失才回退默认。
- 响应见 `AnalysisResult`（§6.4），`filters` 回显 `alliance/hours/minHp/minCount`。

### GET /api/records / POST /api/records/clear
- 全量记录 / 清空。

---

## 9. 打包为自包含 exe（发布）

目标：目标机**无需安装 Node / Python**即可运行。产物在 `build/`（不入库）。

### OCR 服务 — PyInstaller
```bash
python -m pip install --target build/pylibs pyinstaller
cd build/pylibs
python -m PyInstaller --onefile \
  --collect-all rapidocr_onnxruntime \
  ../../server/py/ocr_service.py
```
- `--collect-all rapidocr_onnxruntime` 必须，否则 **onnx 模型文件不打包** → 运行时识别失败。
- 产物 `ocr_service.exe`。

### Web 服务 — esbuild + pkg
```bash
cd server
npx esbuild src/index.ts --bundle --packages=external \
  --platform=node --target=node20 --format=cjs --outfile=build/web/index.cjs
npx pkg build/web/index.cjs --config pkg-config.json --output build/web/web_service.exe
```
- **不要用 `--external=*`**（会把相对模块也外置导致 bundle 仅几 KB），用 `--packages=external` 只 external npm 包。
- `pkg-config.json` 必须通过 `assets` 收集 sharp 的原生平台包：`node_modules/@img/sharp-win32-x64/**`（缺它 → 运行时 `@img/sharp` 模块缺失）。

### 路径适配（重要）
- `src/index.ts` 与 `src/store.ts` 同构处理：
  - 打包环境由 `process.pkg` 识别，`ROOT = path.dirname(process.execPath)`（exe 所在目录，`data/`、`tmp/uploads/` 在 exe 外可写）。
  - 开发环境 `ROOT = path.resolve(__dirname, '..')`。
- `__dirname` 用 try/catch：ESM 走 `import.meta.url`，CJS/exe 回退 `path.dirname(process.execPath)`。

### 一键启停（发布包内）
- `start.bat`：`start ocr_service.exe 5050` + `start web_service.exe`。
- `stop.bat`：`taskkill /IM ocr_service.exe` + `taskkill /IM web_service.exe`。
- 分发用压缩包；说明见发布版 README.md。常见坑见 §11。

---

## 10. 开发与调试脚本

`server/src` 下保留了一批独立调试/验证脚本（`tsx` 直接运行），用于定位具体模块：
- `test-ocr.ts` / `test-parse.ts` / `test-name.ts` 等：OCR 与解析验证。
- `test-check.mjs`：批量回归脚本——遍历测试图，检查修复后无回归（修改识别/清洗规则后建议先跑一遍）。
- `probe-*.ts` / `tmp-*.ts` / `_magatama*.ts` / `_pix-scan.ts` 等：勾玉/红度/布局探针与中间产物，历史调试用途。
- `py/probe_color.py` / `star_*.py`：Python 侧取色 / 星星检测。
- 运行方式：`cd server && npx tsx src/xxx.ts`。
> `star_detect.py` 已确认**不可靠**，仅保留为独立研究工具，**不要**接入主识别链路（避免污染数据）。

---

## 11. 常见陷阱 / 注意事项（重要）

1. **`store.ts` 内存缓存**：改了识别/分析逻辑后必须重启 Web 服务，否则读取的是 `cache` 中的旧数据（`records.json` 已更新但内存未刷新）。
2. **静态 JS 缓存**：前端修改后用户需 **Ctrl+F5** 硬刷新，否则可能加载旧 `app.js`。
3. **背景同盟名单列**：识别同盟名要做到“盟”徽标 + 噪声后缀剔除；`ALLIANCE_NOISE_SUFFIX`（横屏）/ `NOISE_SUFFIX`（竖屏）需随新截图持续补充装饰粘连字（如“画”“飞”）。
4. **中文路径不可读**：OpenCV `cv2.imread` 无法读 Windows 中文路径 → 返回 `None` → 空结果。确保上传/临时路径无中文。
5. **不完整截图剔除**：顶部被 UI 导航栏截断、无同盟名内容行的战斗必须剔除，否则污染统计。判定需**同时**满足“成排 UI 词”+“无内容行”，避免误删完整第一场。
6. **打包后 OCR 崩**：多为模型文件未 `--collect-all` 收集；`sharp` 缺失多为 `pkg-config.json` 未收集 `@img/sharp-win32-x64`。
7. **端口占用**：Web 报 `EADDRINUSE :::3001` → `netstat -ano | findstr :3001` 结束占用进程。
8. **杀软误报**：打包 exe 可能被杀毒拦截，需加入信任区（已在分发包说明中是常见项）。
9. **超大文件**：`server/chi_sim.traineddata`（约 42MB）为历史遗留且重复，当前识别不走 Tesseract，可清理以缩小仓库体积。
10. **服务联调顺序**：先起 OCR（5050）再起 Web（3001）；Web 在 OCR 未就绪时调用识别会报错。
11. **OCR 分隔符 `丨` 识别不稳定**：同盟名中的分隔符 `|` 偶发被漏掉（横屏 `风只因`）或被读成 `一`/`1`（竖屏 `百战一凌云`）。已通过 `cleanAlliance` 的正则归一化缓解（`丨→|`、仅当两侧各 ≥2 字时 `一/1→|`）；新增截图类型时若仍出现，优先补充归一化规则，勿在持久化后手工改数据。
12. **时间格式粘连**：OCR 返回 `2026/08/1509:30:08`（日期与时间间无空格）属正常现象，`TIME_PAT`/`extractTime` 会自动补全；若发现时间未识别，先确认该行是否被顶部截断（时间位于面板顶部右上角，完整第一场常因此缺失），或区域 OCR 是否漏行。
13. **旧数据兼容**：升级前 `records.json` 缺少 `ts/battleTime/hp/compReds` 字段，`loadRecords` 会自动补齐（`ts` 非数字 → null、缺失字段 → 空串、`hp`（`剩余/战前`）用 `splitHp` 拆分为 `hpAfter`/`hpBefore`、`compReds` 缺失 → 空数组）。但旧数据无战报时间/兵力/逐武将红度，时间过滤/兵力筛选/去重/逐武将平均红度对旧记录不生效，需按新规则重新录入。

---

## 12. 快速排查流程（定位问题顺序）

1. 确认端口：`netstat -ano | findstr :5050`、`findstr :3001` 均在监听。
2. 用一张已知正常截图跑 `npx tsx src/test-parse.ts`（复用识别单测脚本思路）看是否解析出武将/同盟/红度。
3. 检查 `data/records.json` 是否正确写入与配对（左/右两条相邻）。
4. 前端异常先硬刷新排除缓存；再断点 `/api/analyze` 返回结构。
5. 若某字段批量识别错误，先定位是该区域的 OCR（粗/精调）问题、匹配表问题，还是后续聚合问题——按「问题定位优先于算法优化」原则处理。